{
  "name": "@apps/html-canvas",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server.ts",
    "build": "bun build ./index.ts --outdir ./dist",
    "start": "bun run server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
